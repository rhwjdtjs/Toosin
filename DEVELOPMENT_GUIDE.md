# 📘 개발 가이드 (Development Guide)

이 문서는 **Toosin** 프로젝트의 1인 개발 워크플로우와 규칙을 정의합니다.

## 🔄 깃허브 워크플로우 (GitHub Workflow)
본 프로젝트는 혼자 개발하지만, 체계적인 관리를 위해 **Git Flow**를 변형하여 사용합니다.

### 1. 브랜치 전략 (Branch Strategy)
*   **`main`**:
    *   언제나 배포 가능한 **MVP 완성 버전** 상태를 유지합니다.
    *   `dev` 브랜치에서 **MVP 단위** 개발이 완료되었을 때만 병합(Merge)합니다. (예: MVP-1 완료 시 Merge)
*   **`dev`**:
    *   **개발 통합 브랜치**입니다.
    *   모든 `feature` 브랜치는 이 곳으로 먼저 병합됩니다.
    *   항상 실행 가능한 상태여야 하지만, 개발 중인 기능이 포함될 수 있습니다.
*   **`feature/*`**:
    *   새로운 기능 개발 시 `dev`에서 생성하는 브랜치입니다.
    *   예: `feature/combat_system`, `feature/ai_basic`, `feature/inventory`
*   **`fix/*`**:
    *   버그 수정 시 생성합니다.
    *   예: `fix/attack_collision`

### 2. 작업 순서 (Process)
1.  **이슈 생성 (Issue)**: 작업할 내용을 이슈 탭에 등록합니다. (예: "플레이어 기본 공격 구현")
2.  **브랜치 생성**: `dev` 브랜치에서 새로운 기능 브랜치를 생성합니다.
    ```bash
    git checkout dev
    git checkout -b feature/new-feature
    ```
3.  **코드 작성 및 커밋**:
    *   커밋 메시지는 명확하게 작성합니다. (아래 컨벤션 참고)
4.  **PR (Pull Request)**:
    *   작업이 완료되면 GitHub에서 **`feature` -> `dev`** 로 PR을 생성합니다.
    *   코드를 검토하고 병합(Merge)합니다.
5.  **MVP 배포 (Merge to Main)**:
    *   하나의 MVP 목표(예: MVP-1)가 `dev`에서 모두 완료되면, **`dev` -> `main`** 으로 PR을 생성하여 병합합니다.

---

## 📝 커밋 컨벤션 (Commit Convention)
일관성 있는 히스토리를 위해 다음 접두어를 사용합니다.

| 태그 | 설명 | 예시 |
| :--- | :--- | :--- |
| **feat** | 새로운 기능 추가 | `feat: Add player jump logic` |
| **fix** | 버그 수정 | `fix: Resolve crash on death` |
| **docs** | 문서 수정 | `docs: Update README` |
| **style** | 코드 포맷팅 (로직 변경 없음) | `style: Edit coding convention` |
| **refactor**| 코드 리팩토링 | `refactor: Optimize AI pathfinding` |
| **chore** | 빌드 수정, 설정 변경 등 | `chore: Update UE5 project settings` |

---

## 📁 파일 관리 규칙
*   **C++ 클래스**: PascalCase (예: `TSCharacter.cpp`, `TSAnimInstance.cpp`)
    *   클래스 접두사 `ATS`(Actor), `UTS`(Object/Component) 등 준수.
*   **변수명**: PascalCase 또는 CamelCase (언리얼 권장: PascalCase)
    *   `Health`, `MaxStamina` 등 파스칼 케이스 권장.
    *   `bIsFalling` (Boolean 접두사 `b`).
*   **블루프린트**: 접두사 `BP_` 사용 (예: `BP_TSCharacter`)
